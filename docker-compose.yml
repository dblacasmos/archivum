services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: archivum
      POSTGRES_PASSWORD: archivum
      POSTGRES_DB: archivum
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: {}